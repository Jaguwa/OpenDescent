name: Release build (transparency)

# On every version tag, attempt an UNSIGNED installer build in public CI from the
# exact tagged source, with SHA-256 checksums and a SLSA build-provenance
# attestation. This is a transparency artifact only: the official download is the
# locally-signed build (signing isn't done in CI). The installer build is
# best-effort — if it can't complete on the runner it won't fail the run.

on:
  push:
    tags: ['v*']

permissions:
  contents: write          # needed to attach the AppImage to a GitHub Release
  id-token: write
  attestations: write

jobs:
  windows-installer:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Build installer (unsigned)
        id: installer
        continue-on-error: true
        run: npx electron-builder --win
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false   # never attempt code signing in CI

      - name: Generate checksums
        if: steps.installer.outcome == 'success'
        run: node scripts/checksums.mjs

      - name: Attest build provenance
        if: steps.installer.outcome == 'success'
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: 'release/*.exe'

      - name: Upload build artifacts
        if: steps.installer.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: opendescent-windows-unsigned
          path: |
            release/*.exe
            release/SHA256SUMS.txt
          if-no-files-found: error

  linux-appimage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Build AppImage (unsigned)
        id: appimage
        continue-on-error: true
        run: npx electron-builder --linux AppImage
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false   # never attempt code signing in CI

      - name: Generate checksums
        if: steps.appimage.outcome == 'success'
        run: node scripts/checksums.mjs

      - name: Attest build provenance
        if: steps.appimage.outcome == 'success'
        uses: actions/attest-build-provenance@v1
        with:
          subject-path: 'release/*.AppImage'

      - name: Publish AppImage to GitHub Release
        if: steps.appimage.outcome == 'success'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Create the release (as a pre-release so it doesn't disturb the
          # Windows auto-updater, which ignores pre-releases). If a release for
          # this tag already exists, just upload/replace the Linux assets.
          gh release create "$GITHUB_REF_NAME" \
            --title "OpenDescent $GITHUB_REF_NAME (Linux)" \
            --notes "Linux AppImage build. Download the .AppImage, then run: chmod +x OpenDescent-*.AppImage && ./OpenDescent-*.AppImage" \
            --prerelease \
            release/*.AppImage release/SHA256SUMS.txt \
          || gh release upload "$GITHUB_REF_NAME" release/*.AppImage release/SHA256SUMS.txt --clobber

      - name: Upload build artifacts
        if: steps.appimage.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: opendescent-linux-appimage
          path: |
            release/*.AppImage
            release/SHA256SUMS.txt
          if-no-files-found: error
